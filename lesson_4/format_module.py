# format_module.py
def f(var1, var2):   
    '''
    Возвращает строку вида "var1 и var2"
    '''
    return '{0} и {1}'.format(var1, var2)
   #return f'{var1} и {var2}' # для версии 3.6 и выше
